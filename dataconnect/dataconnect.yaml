specVersion: "v1"
serviceId: "dandy-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "dandy-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
